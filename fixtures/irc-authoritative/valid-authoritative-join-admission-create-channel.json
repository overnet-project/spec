{
  "description": "An authenticated actor may create a hosted channel on first authoritative JOIN when no authoritative state exists yet.",
  "input": {
    "operation": "authoritative_join_admission",
    "session_config": {
      "authority_profile": "nip29",
      "group_host": "groups.example.test"
    },
    "network": "irc.example.test",
    "target": "#new",
    "authoritative_events": [],
    "actor_pubkey": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
  },
  "expected": {
    "valid": true,
    "assertions": [
      { "path": "admission.0.allowed", "equals": true },
      { "path": "admission.0.create_channel", "equals": true },
      { "path": "admission.0.auth_required", "equals": false },
      { "path": "admission.0.reason", "equals": "" }
    ]
  }
}
