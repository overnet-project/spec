{
  "description": "A discovered hosted channel with no authoritative snapshot fails closed rather than widening authority on empty read.",
  "input": {
    "server_view": {
      "network": "irc.example.test",
      "server_name": "overnet.irc.local",
      "adapter_config": {
        "authority_profile": "nip29",
        "group_host": "groups.example.test"
      }
    },
    "client": {
      "registered": true,
      "nick": "bob"
    },
    "authoritative_channels": {
      "#ops": {
        "scenario": {
          "events": []
        },
        "discovered": true
      }
    },
    "line": "JOIN #ops"
  },
  "expected": {
    "lines": [
      ":overnet.irc.local 473 bob #ops :Cannot join channel (authoritative state unavailable)"
    ]
  }
}
